<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <OutputType>WinExe</OutputType>
    <UseWPF>true</UseWPF>
    <!-- UseWindowsForms is needed transitively because PresentationLogic still uses -->
    <!-- System.Windows.Forms types (MouseEventArgs etc). Removed in Phase 4. -->
    <UseWindowsForms>true</UseWindowsForms>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Common\Common.csproj" />
    <ProjectReference Include="..\PresentationLogic\PresentationLogic.csproj" />
  </ItemGroup>

</Project>
